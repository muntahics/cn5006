# Name: MD Muntahi Hossain  
# Student ID: 2874263
My Reflection for week9's learning:

Today's class was about React Class Component. Class component extends from React.Component class.
It starts with a constructor which takes the properties. Then it modifies the parent class's constructor to update the properties.
"this." keyword is required in order to initialize a state object. It also uses setState method and it takes a callback function to return a new state.
In the example, the class component is using an arrow function. The class components requires a render method to return JSX Elements. 
However, the exportation of the module and using it inside the index.js is similar to functional component.

Screenshot:
<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/bea2ff85-8777-4758-bc31-7554722ef1ba" />
